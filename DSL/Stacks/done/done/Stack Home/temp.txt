typedef struct 
{
int data[MAX];
int top;
}stack;
void strrev(char *ptr,int n){
	for(int i=0;i<n/2;i++){
		char temp;
        temp=*(ptr+i);  
		*(ptr+i)=*(ptr +n-i-1);
		*(ptr +n-i-1)=temp;
	}
}
int empty(stack *s)
{
if(s->top==-1)
return(1);
}
int full(stack *s)
{
if(s->top==MAX-1)
return(1);
return(0);
}
void push(stack *s,int x)
{
s->top=s->top+1;
s->data[s->top]=x;
}
int pop(stack *s)
{
int x;
x=s->data[s->top];
s->top=s->top-1;
return(x);
}
